Allowed actions:
- read project files
- write scoped workflow files
- update state files
- create session logs
- propose exact next steps

Do not:
- claim completion without proof
- expand scope without reason
- edit app runtime code unless the task explicitly requires it
- assume memory outside files in channels/ and state/
